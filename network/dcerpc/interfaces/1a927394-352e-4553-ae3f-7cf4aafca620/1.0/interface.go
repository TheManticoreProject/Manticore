// Package rpcinterface_1a927394352e4553ae3f7cf4aafca620_1_0 is the descriptor for the WdsRpcInterface RPC interface, abstract
// syntax 1a927394-352e-4553-ae3f-7cf4aafca620 version 1.0 ([MS-WDSC]).
//
// Generated by tools/idlgen (descriptor phase) from the IDL, then reviewed:
// the PipeName, the status-code table, and doc comments are not derivable from
// the IDL and must be confirmed by hand (see the TODO markers).
package rpcinterface_1a927394352e4553ae3f7cf4aafca620_1_0

// IDL source: [MS-WDSC] — this interface is translated from and verified
// against the protocol's authoritative IDL. Full IDL (Appendix A):
//   https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-wdsc/d2d48508-bda0-4dce-b6b2-1c314566a9c5
// A fetched copy is kept at ms-wdsc.idl in the interface directory.

import (
	"fmt"

	"github.com/TheManticoreProject/Manticore/network/dcerpc/syntax"
	"github.com/TheManticoreProject/Manticore/windows/guid"
)

// PipeName is retained for descriptor uniformity, but WdsRpcInterface is NOT a
// named-pipe interface: per [MS-WDSC] 2.1 the protocol is carried over ncacn_ip_tcp on a
// dynamic endpoint that the client resolves through the endpoint mapper (TCP/135), and
// authenticated clients MUST use RPC_C_AUTHN_LEVEL_PKT_PRIVACY. This constant is not the
// standard binding for this interface.
const PipeName = `\WdsRpcInterface`

// Opnums for the on-the-wire methods.
const (
	OpnumWdsRpcMessage uint16 = 0
)

// Status codes returned by this interface. WdsRpcMessage returns ERROR_SUCCESS on
// success or a non-zero Win32 error code on failure ([MS-WDSC] 3.1.4.1); the spec does
// not enumerate a fixed set, so the common Win32 codes ([MS-ERREF] 2.2) are provided.
const (
	StatusSuccess uint32 = 0x00000000 // ERROR_SUCCESS

	ErrorAccessDenied     uint32 = 0x00000005 // ERROR_ACCESS_DENIED (5)
	ErrorInvalidData      uint32 = 0x0000000d // ERROR_INVALID_DATA (13)
	ErrorNotSupported     uint32 = 0x00000032 // ERROR_NOT_SUPPORTED (50)
	ErrorInvalidParameter uint32 = 0x00000057 // ERROR_INVALID_PARAMETER (87)
)

// SyntaxID returns the WdsRpcInterface abstract syntax identifier:
// 1a927394-352e-4553-ae3f-7cf4aafca620, version 1.0.
func SyntaxID() syntax.SyntaxID {
	return syntax.SyntaxID{
		UUID:         guid.GUID{A: 0x1a927394, B: 0x352e, C: 0x4553, D: 0xae3f, E: 0x7cf4aafca620},
		MajorVersion: 1,
		MinorVersion: 0,
	}
}

// StatusString returns a mnemonic for the documented status codes, otherwise the
// hex value.
func StatusString(status uint32) string {
	switch status {
	case StatusSuccess:
		return "ERROR_SUCCESS"
	case ErrorAccessDenied:
		return "ERROR_ACCESS_DENIED"
	case ErrorInvalidData:
		return "ERROR_INVALID_DATA"
	case ErrorNotSupported:
		return "ERROR_NOT_SUPPORTED"
	case ErrorInvalidParameter:
		return "ERROR_INVALID_PARAMETER"
	default:
		return fmt.Sprintf("0x%08x", status)
	}
}

// OpnumToName maps each on-the-wire opnum to its method name; the single source of
// truth.
var OpnumToName = map[uint16]string{
	OpnumWdsRpcMessage: "WdsRpcMessage",
}

// NameToOpnum is the reverse of OpnumToName, built at init so the two never drift.
var NameToOpnum = func() map[string]uint16 {
	m := make(map[string]uint16, len(OpnumToName))
	for op, name := range OpnumToName {
		m[name] = op
	}
	return m
}()
