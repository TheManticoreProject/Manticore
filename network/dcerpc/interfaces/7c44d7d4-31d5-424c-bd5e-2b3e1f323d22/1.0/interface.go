// Package rpcinterface_7c44d7d431d5424cbd5e2b3e1f323d22_1_0 is the descriptor for the dsaop RPC interface, abstract
// syntax 7c44d7d4-31d5-424c-bd5e-2b3e1f323d22 version 1.0 ([MS-DRSR]).
//
// Generated by tools/idlgen (descriptor phase) from the IDL, then reviewed:
// the PipeName and the doc comments are not derivable from the IDL and must be
// confirmed by hand (see the TODO markers).
package rpcinterface_7c44d7d431d5424cbd5e2b3e1f323d22_1_0

// IDL source: [MS-DRSR] — this interface is translated from and verified
// against the protocol's authoritative IDL. Full IDL (Appendix A):
//   https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-drsr/3f5d9495-9563-44de-876a-ce6f880e3fb2
// A fetched copy is kept at ms-drsr.idl in the interface directory.

import (
	"github.com/TheManticoreProject/Manticore/network/dcerpc/syntax"
	"github.com/TheManticoreProject/Manticore/windows/guid"
)

// PipeName is retained for descriptor uniformity, but dsaop (the directory-service
// administration interface used for RODC demotion scripting, [MS-DRSR]) is not a
// named-pipe interface: like drsuapi it is carried over ncacn_ip_tcp on a dynamic
// endpoint resolved through the endpoint mapper (TCP/135). This constant is not the
// standard binding for this interface.
const PipeName = `\drsuapi`

// Opnums for the on-the-wire methods.
const (
	OpnumIDL_DSAPrepareScript uint16 = 0
	OpnumIDL_DSAExecuteScript uint16 = 1
)

// Both dsaop methods return a Win32 error code as their DWORD return value, NOT an
// NTSTATUS, so success is ERROR_SUCCESS (0). Those codes are not declared here: the whole
// of [MS-ERREF] 2.2 lives in github.com/TheManticoreProject/Manticore/windows/errors/win32
// as the WIN32_ERROR type, and a subset repeated here would cover a fraction of that
// 2703-code table while drifting from it. Convert a returned status with
// win32.WIN32_ERROR(status) and compare against win32.ERROR_SUCCESS and the rest, which
// also covers the directory-service codes ERROR_DS_* a demotion script can fail with.

// SyntaxID returns the dsaop abstract syntax identifier:
// 7c44d7d4-31d5-424c-bd5e-2b3e1f323d22, version 1.0.
func SyntaxID() syntax.SyntaxID {
	return syntax.SyntaxID{
		UUID:         guid.GUID{A: 0x7c44d7d4, B: 0x31d5, C: 0x424c, D: 0xbd5e, E: 0x2b3e1f323d22},
		MajorVersion: 1,
		MinorVersion: 0,
	}
}

// OpnumToName maps each on-the-wire opnum to its method name; the single source of
// truth.
var OpnumToName = map[uint16]string{
	OpnumIDL_DSAPrepareScript: "IDL_DSAPrepareScript",
	OpnumIDL_DSAExecuteScript: "IDL_DSAExecuteScript",
}

// NameToOpnum is the reverse of OpnumToName, built at init so the two never drift.
var NameToOpnum = func() map[string]uint16 {
	m := make(map[string]uint16, len(OpnumToName))
	for op, name := range OpnumToName {
		m[name] = op
	}
	return m
}()
