// Package rpcinterface_811109bfa4e111d1ab5400a0c91e9b45_1_0 is the descriptor for the winsi2 RPC interface, abstract
// syntax 811109bf-a4e1-11d1-ab54-00a0c91e9b45 version 1.0 ([MS-RAIW]).
//
// Generated by tools/idlgen (descriptor phase) from the IDL, then reviewed:
// the PipeName and the doc comments are not derivable from the IDL and must be
// confirmed by hand (see the TODO markers).
package rpcinterface_811109bfa4e111d1ab5400a0c91e9b45_1_0

// IDL source: [MS-RAIW] — this interface is translated from and verified
// against the protocol's authoritative IDL. Full IDL (Appendix A):
//   https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-raiw/e59461f5-5486-4ec3-9ad6-14b784c1ecd6
// A fetched copy is kept at ms-raiw.idl in the interface directory.

import (
	"github.com/TheManticoreProject/Manticore/network/dcerpc/syntax"
	"github.com/TheManticoreProject/Manticore/windows/guid"
)

// PipeName is the IPC$-relative named pipe for the winsi2 interface. WINS registers a
// single named pipe, \pipe\WinsPipe, shared with the winsif interface ([MS-RAIW] 2.1,
// Standards Assignments).
const PipeName = `\WinsPipe`

// Opnums for the on-the-wire methods.
const (
	OpnumR_WinsTombstoneDbRecs uint16 = 0
	OpnumR_WinsCheckAccess     uint16 = 1
)

// The Win32 error codes both winsi2 methods return ([MS-ERREF] 2.2, [MS-RAIW] 3.2.4) are
// not declared here. The whole of [MS-ERREF] 2.2 lives in
// github.com/TheManticoreProject/Manticore/windows/errors/win32 as the WIN32_ERROR type,
// and a subset repeated here would cover a fraction of that 2703-code table while drifting
// from it. Convert a returned status with win32.WIN32_ERROR(status) and compare against
// win32.ERROR_SUCCESS and the rest. The WINS-specific codes 0x00000FA0-0x00000FA6
// (ERROR_WINS_INTERNAL through ERROR_RPL_NOT_ALLOWED) are rows of that same table and
// need no local declaration either.

// SyntaxID returns the winsi2 abstract syntax identifier:
// 811109bf-a4e1-11d1-ab54-00a0c91e9b45, version 1.0.
func SyntaxID() syntax.SyntaxID {
	return syntax.SyntaxID{
		UUID:         guid.GUID{A: 0x811109bf, B: 0xa4e1, C: 0x11d1, D: 0xab54, E: 0x00a0c91e9b45},
		MajorVersion: 1,
		MinorVersion: 0,
	}
}

// OpnumToName maps each on-the-wire opnum to its method name; the single source of
// truth.
var OpnumToName = map[uint16]string{
	OpnumR_WinsTombstoneDbRecs: "R_WinsTombstoneDbRecs",
	OpnumR_WinsCheckAccess:     "R_WinsCheckAccess",
}

// NameToOpnum is the reverse of OpnumToName, built at init so the two never drift.
var NameToOpnum = func() map[string]uint16 {
	m := make(map[string]uint16, len(OpnumToName))
	for op, name := range OpnumToName {
		m[name] = op
	}
	return m
}()
